angular.module('mobileCarsApp.controllers', [])

.controller('HomeController', function($scope, $state, popupService, $window) {

  /*
  $scope.attributes = Attribute.query(); //fetch all attributes. Issues a GET to /api/attributes

  $scope.deleteAttribute = function(attribute) { // Delete an attribute. Issues a DELETE to /api/attributes/:id

    if (popupService.showPopup('Really delete this?')) {
      attribute.$delete(function() {
        $window.location.href = ''; //redirect to home
      });
    }
  };
  */
});
